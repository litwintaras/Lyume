# PostgreSQL password
POSTGRES_PASSWORD=lyume

# LM Studio URL (running on host machine)
LM_STUDIO_URL=http://host.docker.internal:1234

# Path to embedding model directory on host
EMBED_MODEL_DIR=~/.lmstudio/.internal/bundled-models/nomic-ai/nomic-embed-text-v1.5-GGUF
